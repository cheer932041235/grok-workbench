# 参与 Grok Workbench

欢迎 PR、缺陷报告、文档改进和跨平台测试反馈。不必从一个大功能开始，一个具体问题也能让日常使用更顺手。

## 选择一个改动

小范围修复、错字和补充测试可以直接提交 PR。涉及会话存储格式、ACP 协议、依赖替换或大幅调整交互时，请先开 Issue，说明实际场景和拟议方案。

优先方向见 [改进方向](docs/roadmap.md)。如果认领现有 Issue，请先留言说明准备做什么，方便其他贡献者避免重复工作。

## 报告问题

使用 [Bug 模板](https://github.com/cheer932041235/grok-workbench/issues/new?template=bug_report.yml)，提供应用、Grok CLI 和系统版本，以及复现步骤、预期结果、实际结果。显示问题最好附最小 Markdown 示例和截图。

请移除截图和日志中的私人内容，不要上传账户凭据或完整个人会话目录。一个可复现的小例子比整份日志更容易定位问题。

## 开发流程

1. Fork 仓库，从 `main` 创建描述性分支，例如 `fix/queue-edit`。
2. 按 [开发指南](docs/development.md) 安装依赖并启动桌面版。
3. 一次 PR 聚焦一个问题。改动用户行为时，同时更新相关说明。
4. 运行与改动有关的检查；涉及显示的改动需要实际查看渲染结果。
5. 提交 PR，说明问题、改动后的行为和验证结果。

```sh
npm run check
npm run lint
npm test
npm run build
```

修改 Rust 时另跑 `npm run rust:check`。完整检查使用 `npm run verify`。纯文档修改请检查链接、截图和操作步骤，不要求为文字修改添加测试。

### 不同改动怎样验证

| 改动 | 最有用的证据 |
| --- | --- |
| 公式、代码、表格、布局 | 最小输入、修改前后截图、窗口宽度和字号 |
| 队列、会话管理 | 顺序/暂停/恢复测试、旧记录是否可读取 |
| Grok 交互协议 | CLI 版本、请求场景、是否做过真实 CLI 往返 |
| Rust 进程管理 | 启动、停止、关闭窗口的结果 |
| 文档或安装说明 | 执行过的步骤，尚未验证的平台 |

没有条件进行真实 CLI 或某个平台的测试时，请在 PR 中直说。不要把单元测试通过写成所有环境都验证通过。

## PR 描述

请让首次看到改动的人能读懂：什么操作触发了问题、修改后用户会看到什么、怎样验证、还有哪些场景没覆盖。界面修改附截图。

不需要为简单修复写很长的设计报告。请避免混入无关格式化、大面积重命名、生成文件或安装包。

## 评审与沟通

评审主要关注用户体验、Grok 适配是否正确、旧会话能否继续使用，以及代码是否便于维护。讨论尽量围绕具体行为和证据。

项目由个人维护，回复和合并时间可能不固定。欢迎补充信息或礼貌提醒，也请尊重不同的使用习惯和技术意见。
